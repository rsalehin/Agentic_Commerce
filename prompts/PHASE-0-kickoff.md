# Paste this into Claude Code to start

You are working in the repository `agentic-depot`. Read `CLAUDE.md` completely, then `ROADMAP.md`, then `docs/README.md` and the two source-of-truth specs `docs/03_state-machine.md` and `docs/05_tool-contracts.md`. Do not write code yet.

Task: **Phase 0 — Skeleton.** Produce a plan for P0-01..P0-05 that lists, per task, the files you will create, the interfaces/signatures, the tests, and the commit message. Flag any inconsistency you find between the docs (e.g. a reason code used in rules.yaml that is missing in the state machine) and propose the fix. Wait for my approval before implementing.

Constraints to keep in mind: Windows host (PowerShell), `.venv`, `uv`, FastMCP, ports 8080/8081/8082/5174, `strictPort: true`, conventional commits with task ids, German UI copy / English code.
